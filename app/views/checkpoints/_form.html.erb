<%= form_with model: checkpoint do |form| %>
  <div>
    <%= form.label :Nombre %><br>
    <%= form.text_field :name %>
    <% checkpoint.errors.full_messages_for(:name).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.label :'Descripción' %><br>
    <%= form.text_area :description %><br>
    <% checkpoint.errors.full_messages_for(:description).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <%= fields_for(@checkpoint_roadmap) do |cr| %>
    <div class="field">
      <%= cr.label "Senderos" %> <br>
      <%= collection_select(:roadmaps, :id, @all_roadmaps, :id, :name, {}, {:multiple => true}) %>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>